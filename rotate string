bool rotateString(char* s, char* goal) {
     int n = strlen(s);
    if (n != strlen(goal))
        return false;
    char doubled[205];
    strcpy(doubled, s);
    strcat(doubled, s);

    return strstr(doubled, goal) != NULL;
}
    
