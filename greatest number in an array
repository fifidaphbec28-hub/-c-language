#include <stdio.h>

int main() {
    int i,n,a[10],big;
    printf("enter no of array elements\n");
    scanf("%d",&n);
    printf("enter array elements\n");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    big=a[0];
    for(i=0;i<n;i++)
    {
        if(a[i]>big)
         big=a[i];
    }
    printf("the biggest elements in an array is %d\n",big);
    return 0;
}
